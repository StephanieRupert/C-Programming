#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

//double length(void);
//double width(void);
//double height(void);

//double length = 2;
//double width = 3;
//double height = 4;
//double a;

double length(void);
    {
    printf("Enter length: ");
    scanf("%lf",length);
    return 0;
    }

double width(void);
    {
    printf("Enter width: ");
    scanf("%lf",width);
    return 0;
    }

double height(void);
    {
    printf("Enter height: ");
    scanf("%lf",height);
    return 0;
    }

int main(int argc,char **argv)
    {
    return a = 2 * ((width * length) + (height * length) + (height * width));
    }
